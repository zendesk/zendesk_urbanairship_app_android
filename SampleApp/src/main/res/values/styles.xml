<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="ZendeskSdkTheme">

        <item name="RateMyAppPaddingTop">@dimen/rma_padding_top</item>
        <item name="RateMyAppPaddingBottom">@dimen/rma_padding_bottom</item>

        <item name="android:textAppearance">@style/grey_text</item>
    </style>

    <style name="grey_text" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/zendesk_grey_text</item>
    </style>

</resources>
